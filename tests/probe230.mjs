import{chromium}from'playwright';
const b=await chromium.launch({args:['--no-sandbox']});const p=await b.newPage({viewport:{width:1440,height:900}});p.on('pageerror',e=>console.log('PAGE ERROR',e.message));await p.goto('http://127.0.0.1:8880');await p.waitForFunction(()=>typeof rt!=='undefined'&&!rt.booting);await p.evaluate(()=>{document.querySelectorAll('dialog[open]').forEach(d=>d.close());createUI.tab='settings';navigate(1)});console.log('settings',await p.locator('#main').innerText());await p.screenshot({path:'/home/user/settings230.png'});await p.locator('[data-act="preset-library-open"]').click();console.log('lib',await p.locator('#modal-body').innerText());await p.screenshot({path:'/home/user/library230.png'});await p.evaluate(async()=>{closeModal();await Mio.fileLibrary.hydrateAll();openArtReader(state.books[0].id)});await p.waitForTimeout(1300);console.log('stages',await p.evaluate(()=>({stage:artUI.readingStage?.active,c:$('#reader-canvas')?.clientHeight,w:$('#reader-canvas')?.clientWidth,groups:$$('.mio-reading-stage').length,imgs:$$('#reader-canvas img').map(i=>({w:i.naturalWidth,h:i.naturalHeight,r:i.getBoundingClientRect().toJSON()})).slice(0,3)})));await p.screenshot({path:'/home/user/reader230.png'});await b.close();
