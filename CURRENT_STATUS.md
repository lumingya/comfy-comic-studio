# Mio 2.2.3 · 完整源码运行包

2026-09-14

本版将内容文件、预览与原图导出一致性、独立源素材选择、资源页面导入/导出整合为完整版本，不是此前缺少 `data/` 的整理快照。

## 包含什么

- 实际填充的 `data/`：59 个经审核的内容文件与发行清单；独立画册/原图、分镜、变量、计划、工作流、七种展示模板、市场及演练内容。
- 文件驱动加载，修改和删除不再触发源码内置内容复活；程序 JS、CSS 和 Python 仍分离。
- 相同视口下，预览与导出采用相同版式及实际原图尺寸；横图、竖图、小图完整显示，编辑结果保持原生分辨率。
- HTML、原生画册 ZIP 分别选择附带分镜和变量；接收方在一个窗口分别同意，创建新 ID 和独立图片，不覆盖原资源。
- 画册在画册集/单册菜单分享，分镜在选择器旁分享，变量在设定页分享。设置页保留全局维护与服务。
- 升级教程、完整测试、验收说明与源码构建工具。

## 使用

需要 Python 3.10+。Windows：`start.bat`；macOS/Linux：`bash start.sh`；也可执行 `python server.py`。日常运行无需 Node.js。

**已有工作区：将新版解压到新程序目录，再用 `MIO_DATA_DIR` 指向原来的 `data/`。不要把随包初始内容覆盖解压到自己的唯一工作区。** 1.x 旧格式需显式转换；2.1 原生工作区直接保留。

详见 [内容、分享与升级教程](docs/guide/CONTENT_AND_SHARING.md)、[发布说明](docs/RELEASE_2_2_0.md) 和 [本版验收记录](docs/acceptance-2.2.3/README.md)。

发行包不含个人密钥、任务记录、缓存、旧备份或 `node_modules`。未知付费请求不自动重发；删除仍只需要一次确认。

本次修复：预设选择器旁可删除所选预设。保留已应用变量、逐幕覆盖、参考图、原画册和任务快照。见 [2.2.1 更新说明](docs/RELEASE_2_2_1.md)。单文件可安全追加不同 ID 的资源，但不会阻止操作系统替换同路径文件。

本次修复：预设选择、独立草稿及更新目标一致；切换不丢草稿，另存为创建独立副本，应用才复制到本册。保留删除功能。见 [2.2.2 更新说明](docs/RELEASE_2_2_2.md)。

2.2.3：画册菜单「重命名」支持逐本编辑完整现有名称；保留已有标签及搜索用途，移除重命名中的标签输入。队列新增连续失败暂停上限，默认 5，0 可关闭，达到后暂停当前任务而非继续逐幕尝试。见 [本版说明](docs/RELEASE_2_2_3.md)。
