# 架构与交互重构 · 验收记录

版本：**3.1.0-dev.2**。日期：2026-09-15。

## 当前回归

| 检查 | 结果 |
| --- | --- |
| 构建、JS / HTML lint | 通过 |
| JS 契约 | 56 / 56 |
| Python | 381 项：377 通过、4 跳过 |
| 新架构交互浏览器验收 | 37 / 37 |
| 装配生产浏览器验收 | 39 / 39 |
| 自适应阅读 | 26 / 26 |
| 展示与阅读 | 42 / 42 |
| 图片编辑与资源 | 30 / 30 |
| 实际源码 ZIP 解压运行 | 18 / 18 |

复现：`npm run build && npm run test:current`。交付包复现：`node tests/packaged_runtime.mjs /path/to/mio-3.1.0-dev.2-source.zip`。

## 本轮的 37 项浏览器检查

实际点击和填写表单，检查新导航、默认开关、关闭后的子项/入口/有效权限、单一偏好持久化、市场全页与实际搜索结果、视觉审校唯一 API 入口及轻量面板。

向导检查渠道记忆、非 ComfyUI 隐藏种子、无映射禁用、有有效映射仍默认不启用、手动启用后才可编辑。确认向导任务不产生模型调用。

画布用真实鼠标拖拽建立 A→1/2、B→2/3，并移动节点。检查服务器实际保存的两张待命卡和冻结快照：各自包含两个正确的预设，没有调用模型。移动端检查向导适配和画布内部滚动，不把模态框撑出视口。

还验证了主编辑区正负提示词、源分镜全幕负向覆盖、作品简介默认折叠，以及预设专属负向/裸 JSON 编辑器不存在。

截图：`acceptance-architecture/`；生产、阅读及包运行证据：`acceptance-assembly/`。截图不计入源码 ZIP。

## 服务端新增验证

- 帧负向提示词仅通过显式普通变量插值，旧预设 `negative` 不被混入请求。
- 种子映射检查实际节点与数字输入。
- 明确选择的保存工作流覆盖当前全局工作流；关闭种子时保留字面量 731，开启时编译为指定种子 8，源工作流不变。
- 批量装配验证失败和控制索引提交失败都不发布半批可运行任务。
- 既有队列检查仍覆盖范围冻结、幂等、后缀/单幕重跑、失败保图、原生发布失败恢复，以及不可变对象校验。

## 诚实边界

所有模型调用验收均为受控测试返回，不代表真实模型画质或真实账单验证。真实付费服务、用户复杂 ComfyUI 工作流及 Windows 未实机验证；Python 有既有 SQLite 关闭警告。

历史 `npm test` 仍包含要求分镜持有预设/工作流选择器的旧所有权用例；不把它包装成全绿，也不为过期测试恢复旧架构。当前回归只覆盖表中列出的范围。旧创作控制器尚未完全删除，后台结果对象垃圾回收未实现，无自动变量命名空间。

没有 GitHub 推送，没有迁移或覆盖已有用户生产数据。
