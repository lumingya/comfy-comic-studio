# 常见问题

## 连接失败

核对服务地址、端口和服务是否启动。使用 ComfyUI 时点击「检查连接」。

## 获取模型失败

核对基础地址和 API Key；也可直接输入模型 ID。

## 生成失败

打开任务的诊断详情，查看 HTTP 状态与上游错误，核对模型、参数和额度。

## 结果未确认

使用任务 ID 在上游核对结果，再选择恢复或重跑。

## 保存失败

检查本地服务、目录权限和剩余磁盘空间，随后重试保存。

## 导入失败

检查文件格式。ComfyUI 工作流使用 API 格式 JSON；画册和带图片的资源使用对应 ZIP 导入入口。
