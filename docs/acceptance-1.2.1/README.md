# 1.2.1 — 画面优先的阅读修订

2026-09-13。方向来自用户对真实漫画截图的反馈，并选择「画面优先，干净连贯」。这不是新增模板，而是修订原内置 AFTERGLOW。

## 旧包复现

`baseline-repro.txt` 来自实际交付的 1.2.0 ZIP 中携带的离线画册，不是重新拼出的假旧样式。1440×1000 视口下，竖图宽 439px、方图宽 768px，比例约 1.749；竖图旁白确实放在图片侧面。

## 新版结构验证

`afterglow.txt` 共 42 项通过，保留上版 33 项检查，增加：

- 旧内置模板更新到设计修订 2，保留自定义副本与画册选择。
- 图像同轴居中，说明区在图下，并贴合图片边缘。
- 取消旧版竖图窄画框；常规尺寸同上限，小图仍不放大。
- 正文移除大号编号、游离的 PLATE 标签和通用卷首文案。
- 24 幕交替竖图/方图、相同原图宽度的密集漫画测试：各幕宽 840px，保持原始比例，说明与下一幕之间距离一致。
- 390px 手机视口正文宽约 363px、320px 窄屏无横向溢出。
- 禁用 JavaScript 时同样保持等宽连续布局。

密集测试用两张基于本项目 AI 插画制作的多格/对白素材交替组成，图片上标注“阅读排版测试”。没有使用或分发用户上传截图，不声称已验证用户的原始 24 幕工程。

## 视觉检查

已实际打开并查看桌面正文衔接、手机正文和夜间正文截图；也查看了新普通插画的竖图阅读和手机衔接。三张密集正文截图收录在本目录。没有用漂亮封面代替正文验收。

## 回归和完整包

- `full-test.txt`：最终 npm test 退出 0。
- `runtime-before.sha256` / `runtime-unchanged.txt`：完整回归前后运行文件一致。
- `docs.txt`：12 项文档及本地文档服务测试通过。
- `baseline-diff.txt`：与真实 1.2.0 包逐文件比较，后端 Python、主 styles.css、图片编辑器未改。
- 最终 ZIP 另从中文带空格目录解压启动，验证旧画册、旧内置模板启动迁移、自定义副本保留、新版预览/导出、编辑、删除和无关用户文件。最终日志随交付目录另附。

原有队列、删除一次确认、迟到结果、持久记录与图片编辑回归继续执行，未为了此模板更改这些机制。

## 边界

实际环境 Linux + Playwright Chromium；手机是模拟视口。4 项 Windows 原生测试明确跳过。未进行实体手机、原生 Windows/macOS、Safari/Firefox、真实付费供应商验收；没有付费生成请求。测试通过不等于所有人都会喜欢新版审美。
