# Mio 2.2.1 验收记录

## 本次范围

- `full-suite.txt`：完整回归通过。JavaScript 56 项；Python 295 项（291 通过、4 项 Windows 平台限定测试跳过）；20 组浏览器测试共 629 项检查，其中新增预设删除 23 项。
- `preset-delete.txt`：实际预设选择器，新建后删除、取消、全册/单幕继承与覆盖、第二份计划、参考图所有权、原画册与入队快照不变、仅变量画册再次导出、重载不复活。
- 异常路径包含受控 409、500、已提交但响应丢失及普通保存重试；失败时不误报删除成功。
- 复制路径验证：不同文件名和 ID 追加；只改文件名但同 ID 报冲突，原文件未被程序覆盖。
- `upgrade-2.2.0.txt`：从实际交付的 2.2.0 包创建并编辑的工作区升级。所有已有原生资源及设置文件逐字节保留，用户预设/画册内容不变，新删除入口可用。
- `packaged-runtime.txt`、`packaged-preset-delete.txt`：最终 ZIP 的逐文件哈希校验、实际解压启动和实际解压项目上的预设删除复核。

2.2.0 升级基线 SHA-256：`48f45e0a7d0e1d81c1753643dc18187a80adb7aba4d6de00a9029d6a53dcfc79`。

当前完整回归中，较旧的 2.1 原始 ZIP 对照因未携带旧包而明确跳过一项；本次另行执行了实际 2.2.0 → 2.2.1 升级检查，不将历史测试数量冒充本次运行结果。

截图覆盖桌面及 320/390px、明暗主题的真实预设工具栏和单次删除确认。参考图片来自随包示范，不是本次模型生成结果。

验证环境为 Linux / Chromium，使用隔离临时工作区。没有付费模型请求；未声称验证实体手机、Windows/macOS 桌面实机或所有供应商。
