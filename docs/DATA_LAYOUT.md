# 数据目录与备份（2.0）

当前主程序使用独立文件，不再使用 `albums/index.json`、`storyboards/templates.json` 等聚合文件。

请阅读[独立文件工作区教程](guide/FILE_LIBRARY.md)：包含实际目录树、启动路径、JSON 与图片侧目录、独立分享包、按文件版本保存、密钥、完整备份和性能实测。

旧版升级请使用[一次性离线转换](guide/FILE_LIBRARY_CONVERSION.md)，输出到新目录。不要按旧教程直接覆盖唯一的数据目录。
