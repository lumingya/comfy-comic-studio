# Mio 2.2.2 验收记录

## 原始交付包对照

- `baseline-reproduction.txt`：直接运行实际交付的 2.2.1 ZIP 解压项目。新建空白预设、另存、添加变量、切换后显示空白；原七海文件仍有 `nanami`。随后实际点击更新，确认写入了之前新建的预设而不是下拉框所示对象。不是仅凭截图推断，也不是只做静态代码检查。
- `upgrade-2.2.1.txt`：用旧包创建并编辑原生工作区，再以新版启动同一数据目录。所有已保存原生资源和设置文件逐字节相同；编辑过的预设与画册仍可见；新版选择预设显示其已有变量，「另存为」和删除入口可用。3 项检查通过。
- 原始 2.2.1 包 SHA-256：`05d484405023a4d28e0c7edb70f3ba348423655ad2aef019ac138d74fd40517e`。

## 本次专项

`preset-isolation.txt`：40 项实际浏览器检查通过，包括：

- 原操作序列的复核；本册/原预设/其他草稿/画册/队列不串写；原预设文件逐字节不变。
- 另存之后的更新目标、正常切换/刷新后的文本与图片草稿、布尔/数值/JSON、属性删除和独立导出。
- 应用取消、应用复制、后续预设更新不影响本册；旧的全局与单幕引用保持原有效果和覆盖顺序。
- 延迟上传后切换预设、删除后重建同名属性、删除整份预设后的迟到结果。
- 受控 409、500、服务端已提交但响应丢失、普通更新重试的身份与幂等性。
- 真正修改磁盘上的原预设，再重载旧草稿；拒绝覆盖外部新内容，另存为可保留草稿。
- 新建/另存失败不虚报成功、不改本册，重试仍使用同一 ID；取消不创建。
- 实际从当前草稿下载原生变量 ZIP，后端再次检查通过，包含所属参考图片字节。
- 1440px 明色与 390px 暗色的编辑目标说明、字段和工具栏；无浏览器异常或付费生成提交。

`preset-delete.txt`：沿用 2.2.1 的 23 项删除回归，包括全局/单幕继承、图片归属、原画册与冻结快照、仅变量画册再次导出、409/500/响应丢失及重试、不同 ID 文件追加与重复 ID 冲突、320/390px 明暗布局。新建预设现在不自动应用，测试明确点击应用后再验证已应用值的保留，没有取消原有的保值断言。

## 完整回归与完整包

本次完整回归退出码为 0：JavaScript 56 项通过；Python 295 项（291 通过、4 项 Windows 专属跳过）；21 组浏览器测试共 669 项检查通过。完整输出见 `full-suite.txt`；构建与静态检查见 `build.txt`、`lint.txt`。实际 ZIP 解压运行、59 份内容哈希、原图及分享/导入复核见 `packaged-runtime.txt`。实际解压项目上的专项复核见 `packaged-preset-isolation.txt` 与 `packaged-preset-delete.txt`。

旧 smoke 测试中「新建空白」的检查改为检查当前预设编辑区，而不是要求清空本册；更新目标检查改为下拉框选择。新增专项另行断言本册及其他预设的内容保持原样。截图在有限入场动画结束后拍摄，不通过修改生产 CSS 或隐藏错误来让截图通过。

## 环境与边界

Linux、Python 3.13、Playwright/Chromium；隔离临时工作区，原始交付包只读取/解压。没有请求付费模型；不声称验证实体手机、Safari/Firefox 或 Windows/macOS 实机。完整 Python 回归中的 Windows 专属项在 Linux 上明确跳过。较旧 2.1 ZIP 的可选对照若缺失会明确跳过，本次使用上面的实际 2.2.1 包另做升级对照。

本版保护后续编辑和升级，不猜测或自动恢复过去已经被误写且没有原始副本的数据。
