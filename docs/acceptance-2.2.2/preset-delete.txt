PASS preset delete is beside the selector and disabled without a selection
PASS new blank preset is selected and can immediately be deleted
PASS cancel retains the preset draft and leaves the album plan unchanged
PASS one confirmation deletes the file while keeping applied blank values
PASS deleted preset stays deleted after reload
PASS lazy album summary exposes only preset IDs needed for safe deletion
PASS 1440 light selector actions fit the viewport
PASS 390 dark selector actions fit the viewport
PASS 320 light selector actions fit the viewport
PASS confirmation discloses references and retains used values, not a second abandonment prompt
PASS global and per-scene precedence remains identical after detaching the deleted preset
PASS other presets, queued snapshots and unrelated album summaries remain unchanged
PASS used reference images belong to surviving owners, not the deleted preset
PASS retained reference image bytes are readable
PASS retained reference image bytes are readable
PASS reload keeps the preset deleted and variables-only album exportable
PASS 409 leaves original disk resource and an explicit unconfirmed local draft
PASS 500 never claims a completed deletion or discards the local draft
PASS lost acknowledgement is shown as unconfirmed even if the server committed deletion
PASS normal save retry is idempotent and retains the copied reference
PASS a distinct native file and ID append a preset without overwriting the original
PASS same ID under another filename is diagnosed, not silently merged or treated as a new preset
PASS no browser exceptions or generation submissions
23 preset deletion checks PASS
