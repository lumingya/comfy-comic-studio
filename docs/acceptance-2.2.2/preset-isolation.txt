PASS exact reported flow returns to the original preset content, not the new blank editor
PASS original preset, current album plan, books and queued snapshots remain unchanged
PASS new preset B retains its own draft across switching
PASS Save As creates a separate C draft and owns the subsequently added variable
PASS Update saves only the preset named in the selector
PASS a delayed image upload stays with C after switching to B
PASS C shows its own uploaded image and text draft
PASS per-preset drafts survive a normal saved reload
PASS canceling Apply leaves the album plan alone
PASS Apply copies the visible draft into this album without changing the preset or other albums
PASS later preset updates do not mutate already applied album values
PASS empty selector shows the actual album settings and no preset update target
PASS editing the album does not write back into C
PASS Save As rebinds Update to D, never the source C
PASS all original preset files remain byte-identical after the entire reproduced edit/switch/save sequence
PASS 409 leaves the preset file unchanged, retains its draft and never reports confirmed success
PASS 409 ordinary Update retry saves the same selected preset only
PASS 500 leaves the preset file unchanged, retains its draft and never reports confirmed success
PASS 500 ordinary Update retry saves the same selected preset only
PASS a committed save with lost acknowledgement stays explicitly unconfirmed
PASS ordinary Update retry after lost acknowledgement is idempotent
PASS reopening preserves both the disk edit and the independent pending draft
PASS stale draft is rejected before overwriting a newer preset
PASS Save As recovers the stale draft without replacing either original
PASS late upload cannot bind to a removed-and-recreated property with the same key
PASS removing a draft field leaves the same field in C intact
PASS boolean, number and JSON fields stay with the selected preset draft
PASS actual draft export ZIP passes inspection and carries its owned image bytes
PASS export takes the visible preset draft rather than the album or source preset
PASS updating a legacy referenced preset preserves the album’s effective values and local precedence
PASS canceling Save As neither creates a file nor changes the selected target
PASS 另存 failure retains an independent unconfirmed copy without changing the album
PASS 另存 retry persists the same ID without creating duplicates
PASS 新建 failure retains an independent unconfirmed copy without changing the album
PASS 新建 retry persists the same ID without creating duplicates
PASS deleting a preset during upload does not resurrect it or attach its image to another editor
PASS the requested concise Save As label is used
PASS editor ownership and controls fit 1440px light
PASS editor ownership and controls fit 390px dark
PASS no browser exceptions or paid generation submissions
40 preset isolation checks PASS
