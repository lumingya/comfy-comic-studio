import {createRequire} from 'node:module';import fs from 'node:fs';const require=createRequire(import.meta.url);const {parse}=require('./acorn.cjs');
try{const result={};for(const [key,source] of Object.entries(JSON.parse(fs.readFileSync(0,'utf8')))){const ast=parse('async function __macro(){\n'+source+'\n}',{ecmaVersion:'latest'});const deps=new Set();function walk(n){if(!n||typeof n!=='object')return;if(n.type==='CallExpression'&&n.callee.type==='MemberExpression'&&n.callee.object.name==='ctx'&&((!n.callee.computed&&n.callee.property.name==='get')||(n.callee.computed&&n.callee.property.value==='get'))){if(n.arguments[0]?.type==='Literal'&&typeof n.arguments[0].value==='string')deps.add(n.arguments[0].value)}for(const [k,v] of Object.entries(n)){if(k==='start'||k==='end')continue;if(Array.isArray(v))v.forEach(walk);else if(v&&typeof v==='object')walk(v)}}walk(ast);result[key]=[...deps]}process.stdout.write(JSON.stringify(result))}catch(e){process.stderr.write(e.message);process.exit(1)}
